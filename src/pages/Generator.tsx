import { useRef, useState } from 'react';
import { SealCanvas, SealCanvasHandle } from '../components/SealCanvas/SealCanvas';
import { SealForm } from '../components/SealForm/SealForm';
import { defaultBorderOpts, defaultCenterTextOpts, defaultFiveStarOpts, defaultInnerBorderOpts, defaultInnerLoopLineOpts, defaultOpts, defaultSerNoOpts, defaultSubTextOpts, defaultTextOpts, Options } from '../seal';
const initial: Options = { ...defaultOpts, type: 'company', shape: 'circle', text: { ...defaultTextOpts, text: '超级无敌爱国创新科技有限公司' }, subText: { ...defaultSubTextOpts, text: '合同专用章' }, serNo: { ...defaultSerNoOpts }, fiveStar: { ...defaultFiveStarOpts, color: 'red' }, border: { ...defaultBorderOpts, color: 'red' }, innerBorder: { ...defaultInnerBorderOpts, color: 'red' }, innerLoopLine: { ...defaultInnerLoopLineOpts, color: 'red' }, centerText: { ...defaultCenterTextOpts, text: '测试文案' } };
export function Generator() { const [options, setOptions] = useState<Options>(initial); const [copied, setCopied] = useState(false); const canvas = useRef<SealCanvasHandle>(null); const copyConfig = async () => { await navigator.clipboard?.writeText(JSON.stringify(options, null, 2)); setCopied(true); window.setTimeout(() => setCopied(false), 1500); }; return <main className="configurator"><div className="preview-panel"><div className="preview-stage"><SealCanvas ref={canvas} options={options} /></div></div><div className="configuration"><div className="configuration-header"><h1>配置面板</h1><div><button className="text-button" onClick={() => canvas.current?.download()}>下载印章</button><button className="text-button" onClick={copyConfig}>{copied ? '已拷贝' : '拷贝配置'}</button></div></div><SealForm options={options} onChange={setOptions} /></div></main>; }
