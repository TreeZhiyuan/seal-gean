import {useRef,useState} from 'react'; import {SealCanvas,SealCanvasHandle} from '../components/SealCanvas/SealCanvas'; import {SealForm} from '../components/SealForm/SealForm'; import {defaultOpts,defaultTextOpts,defaultSubTextOpts,defaultSerNoOpts,defaultFiveStarOpts,defaultBorderOpts,defaultInnerBorderOpts,defaultInnerLoopLineOpts,defaultCenterTextOpts,Options} from '../seal';
const initial:Options={...defaultOpts,type:'company',shape:'circle',text:{...defaultTextOpts,text:'示例有限公司'},subText:{...defaultSubTextOpts,text:''},serNo:{...defaultSerNoOpts},fiveStar:{...defaultFiveStarOpts,color:'red'},border:{...defaultBorderOpts,color:'red'},innerBorder:{...defaultInnerBorderOpts,color:'red'},innerLoopLine:{...defaultInnerLoopLineOpts,color:'red'},centerText:{...defaultCenterTextOpts,text:''}};
export function Generator(){const [options,setOptions]=useState(initial);const canvas=useRef<SealCanvasHandle>(null);return <main><div><h1>印章生成器</h1><SealForm options={options} onChange={setOptions}/><button onClick={()=>canvas.current?.download()}>下载 PNG</button></div><SealCanvas ref={canvas} options={options}/></main>}
