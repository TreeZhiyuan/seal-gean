# seal-generator

基于 TreeZhiyuan/seal Canvas 核心算法的 React + TypeScript + Vite 印章生成器。

## 开发

```bash
nvm use 24.19.0
pnpm install
pnpm dev
pnpm lint
pnpm build
```

Open API 目录已预留在 `src/api`，第一阶段不主动调用外部接口。
